# Copy to secrets.properties (gitignored) and fill in your own values.
# Create the token in HA: Profile -> Security -> Long-lived access tokens.
haUrl=http://YOUR_HA_IP:8123
haToken=YOUR_LONG_LIVED_ACCESS_TOKEN
